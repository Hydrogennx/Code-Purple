package com.hydrogennx;

/**
 * These are AttackTypes that, instead of being created for the attacker, are created for the defender.
 * They are very much the same, and the protocol behind this should probably be changed.
 */
public enum DefenseHelper {

    PROTECT;

}
